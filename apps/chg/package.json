{
  "name": "chg-crm",
  "version": "1.0.0",
  "description": "Cleveland Holding Group CRM — frontend workspace (server extracted to /server)",
  "private": true,
  "scripts": {
    "build": "cd client && npm run build",
    "start:client": "cd client && HOST=0.0.0.0 PORT=5000 DANGEROUSLY_DISABLE_HOST_CHECK=true npm start",
    "dev": "npm run start:client",
    "build:prod": "cd client && npm install && npm run build",
    "seed": "node scripts/seed.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/chg2026/rei-code.git"
  },
  "license": "ISC",
  "dependencies": {
    "@supabase/supabase-js": "^2.49.4",
    "dotenv": "^16.4.7"
  }
}
