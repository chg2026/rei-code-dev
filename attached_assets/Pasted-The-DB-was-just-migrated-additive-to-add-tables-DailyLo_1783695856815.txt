The DB was just migrated (additive) to add tables DailyLog, Issue, PunchItem, Photo and enums IssueType (Issue|Question), IssueStatus (Open|InProgress|Resolved), PunchStatus (Open|Done). First update prisma/schema.prisma to match exactly — add those four models (fields as in the migration) and enums, with relations: each has project Project @relation(onDelete: Cascade); Issue/PunchItem have phase Phase? @relation(onDelete: SetNull); Photo has document Document? @relation(fields:[docId], onDelete: SetNull); add matching back-relations on Project (and Phase where applicable, and Document for photos). assigneeId/createdById/dailyLogId/issueId/punchItemId are plain String? (no relations). Then npx prisma generate. Read components/rehab/TabNav.tsx, the project layout.tsx, lib/rehab/actions.ts#uploadProjectDocument, and how Activity entries are written. Add auth + can() to all new routes. Build "Phase 3 — Field Execution":
1. Daily Logs — a new project tab "Daily Log": list by date, "+ New entry" (date, weather, crew count, work performed, notes), with optional photo attachments. CRUD via /api/rehab/[projectId]/daily-logs.
2. Issues (incl. Questions/RFI-lite) — a new "Issues" tab: list with filters by status and type; "+ New" (type Issue or Question, title, description, linked job type optional, assignee optional); change status Open → In Progress → Resolved; optional photos. CRUD via /api/rehab/[projectId]/issues.
3. Punch List — a new "Punch List" tab: list of items (title, location, linked job type optional, assignee optional), toggle Open/Done, optional photos. CRUD via /api/rehab/[projectId]/punch.
4. Photos — reuse uploadProjectDocument to store the file as a Document (category "Photo"), then create a Photo row (with docId, caption, and whichever of phaseId/dailyLogId/issueId/punchItemId applies). Add a project "Photos" tab showing a gallery (newest first), filterable by job type. Attaching a photo from a daily log / issue / punch item links it to that record.
5. Wire into Overview + Activity. Add Overview tiles: Open issues count, Open punch items count, and latest daily log date. Write an Activity entry when a daily log, issue, or punch item is created or resolved, so Activity becomes the unified stream.
Then run prisma generate && next build, confirm no ESLint errors, and self-review that nothing regresses existing tabs.