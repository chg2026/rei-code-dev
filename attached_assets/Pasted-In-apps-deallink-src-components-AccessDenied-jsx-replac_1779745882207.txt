In apps/deallink/src/components/AccessDenied.jsx, replace the entire file content with:

import React from 'react';
import { Link } from 'react-router-dom';
import { useAuth } from '../context/AuthContext.jsx';
import { Kicker } from './UI.jsx';

export default function AccessDenied() {
  const auth = useAuth();
  const hasProfile = !!auth.profile;

  return (
    <div className="center-grid">
      <div className="pane left">
        <Link to="/" style={{ fontFamily: 'var(--mono)', fontSize: 11, letterSpacing: 1.6, textTransform: 'uppercase' }}>REI Flywheel</Link>
        <div>
          <div className="serif" style={{ fontSize: 'clamp(28px, 4vw, 40px)', lineHeight: 1.05 }}>
            One link for<br />every deal<br />you wholesale.
          </div>
        </div>
        <Kicker>© 2026 · Doorine</Kicker>
      </div>
      <div className="pane right" style={{ display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
        <div style={{ maxWidth: 360, width: '100%', textAlign: 'center' }}>
          {hasProfile ? (
            <>
              <Kicker>Access denied</Kicker>
              <div className="serif" style={{ fontSize: 26, marginTop: 8 }}>No REI Flywheel access.</div>
              <p style={{ marginTop: 14, color: 'var(--mute)', fontSize: 13, lineHeight: 1.6 }}>
                Your Doorine account ({auth.profile?.email || auth.profile?.phone}) doesn't have
                an active REI Flywheel entitlement. Contact your account admin to enable it.
              </p>
              <div style={{ marginTop: 20, display: 'flex', gap: 8, justifyContent: 'center' }}>
                <button className="btn sm" onClick={() => auth.signOut()}>Sign out</button>
                <Link to="/" className="btn sm solid">Back home</Link>
              </div>
            </>
          ) : (
            <>
              <Kicker>Not registered</Kicker>
              <div className="serif" style={{ fontSize: 26, marginTop: 8 }}>No Doorine account found.</div>
              <p style={{ marginTop: 14, color: 'var(--mute)', fontSize: 13, lineHeight: 1.6 }}>
                This number or email isn't registered on Doorine yet.
                Sign up to create your REI Flywheel account.
              </p>
              <div style={{ marginTop: 20, display: 'flex', gap: 8, justifyContent: 'center' }}>
                <button className="btn sm" onClick={() => auth.signOut()}>Sign out</button>
                <Link to="/" className="btn sm solid">Sign up</Link>
              </div>
            </>
          )}
          {(auth.user?.email || auth.user?.phone) && (
            <div style={{ fontSize: 11, color: 'var(--dim)', fontFamily: 'var(--mono)', marginTop: 16 }}>
              Signed in as {auth.user.email || auth.user.phone}
            </div>
          )}
        </div>
      </div>
    </div>
  );
}

Also in apps/deallink/src/pages/Login.jsx, find:
  Need an account? Sign up via <a href="/signup" ...>Gold Bridge</a> and ask your admin to enable Deal Link.

Replace with:
  New to REI Flywheel? <Link to="/" style={{ textDecoration: 'underline', color: 'var(--ink)' }}>Sign up here</Link> with your phone number.

Then run: npm run build --workspace=apps/deallink