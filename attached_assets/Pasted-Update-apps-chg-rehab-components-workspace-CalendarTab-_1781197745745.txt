Update apps/chg-rehab/components/workspace/CalendarTab.tsx to show task names inline on calendar cells instead of dots, with a hover + button. Spec:

1. Replace the calDots/calDot rendering inside each cell with this:
   - Show up to 3 event chips per cell. Each chip is a small pill showing the event title (truncated to ~22 chars with ellipsis).
   - If e.link exists, wrap in a <Link href={e.link}>. Otherwise a <span>.
   - Each chip gets a left color bar using KIND_COLORS[e.kind] — use borderLeft: `3px solid ${KIND_COLORS[e.kind] ?? "#6366f1"}`.
   - If there are more than 3 events, show a "+N more" grey text below the chips.
   - Stop click propagation on the chip so clicking a task chip doesn't trigger the day's onClick (which opens CreateTaskModal).

2. Show a hover + button on each in-month cell:
   - Add a small absolutely-positioned "+" button in the top-right corner of the cell, visible only on hover (use CSS: opacity 0 normally, opacity 1 on parent:hover via inline style or a CSS class).
   - Clicking it calls setCreateDate(ymd(c.date)) — same as the current day onClick.
   - Remove the current day onClick so the whole cell is no longer a click target — only the + button triggers task creation.

3. Add these CSS classes to styles.module.css (workspace):
   .calEventChip { display: block; font-size: 11px; padding: 1px 4px 1px 6px; margin: 1px 2px; border-radius: 3px; background: rgba(99,102,241,0.08); color: var(--ink); white-space: nowrap; overflow: hidden; text-overflow: ellipsis; max-width: 100%; text-decoration: none; cursor: pointer; }
   .calEventChip:hover { background: rgba(99,102,241,0.15); }
   .calAddBtn { position: absolute; top: 2px; right: 2px; width: 18px; height: 18px; border-radius: 4px; border: none; background: var(--marine); color: #fff; font-size: 14px; line-height: 1; cursor: pointer; display: flex; align-items: center; justify-content: center; opacity: 0; transition: opacity 0.1s; }
   .calCell { position: relative; } /* ensure position relative for absolute child */
   .calCell:hover .calAddBtn { opacity: 1; }

4. Make sure the calCell div already has className including s.calCell (it does) so the hover CSS works.

Run tsc --noEmit and report errors.