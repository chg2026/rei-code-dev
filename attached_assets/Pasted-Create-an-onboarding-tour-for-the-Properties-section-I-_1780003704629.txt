Create an onboarding tour for the Properties section. I need two new component files and edits to three existing files. Here's exactly what to build:
File 1 — create apps/deallink/src/components/OnboardingCard.jsx:
A floating card component. Fixed position, bottom-right corner (bottom: 88px, right: 24px), width 360px, white background, border-radius 16px. It reads a stepKey prop. There are 5 steps: properties_list (Step 1 — 'Your deal inventory' — 'Properties is where you manage all your active deals. Add a deal manually, import a CSV, and track asking price, ARV, and status — all in one place.'), deal_overview (Step 2 — 'Set up your deal' — 'Fill in the address, specs, pricing, and photos for this property. The Live Preview on the right shows exactly what buyers will see on your public profile.'), deal_analysis (Step 3 — 'Run the numbers' — 'Run a Deal Analyzer on this property to calculate ARV, repair cost, and MAO. The results attach to this deal and can be shown to buyers in the Investment Memo.'), deal_documents (Step 4 — 'Keep docs with your deal' — 'Upload contracts, inspection reports, title docs — any file tied to this property. You can control which documents buyers see through the Investment Memo.'), deal_im (Step 5 — 'Share with buyers' — 'The Investment Memo is a public page buyers can open without logging in. Toggle which sections to include, copy the link, and send it straight to your buyer list.'). Each step has an empty videoUrl field (placeholder). The card auto-hides if the step is already 'complete' or 'dismissed' in localStorage under key rei_flywheel_tour. Top of card has a gold (#b8860b) progress accent bar that fills to the current step percentage. Header shows 'Step X of 5' in gold, and an X button to dismiss. Body shows title, description, a 16:9 video area (iframe if videoUrl is set, otherwise a dark placeholder with a gold play icon and text 'Video coming soon'). Two buttons: 'Mark as done' (gold #b8860b, full width) and 'Skip' (gray). Mark as done saves status 'complete', Skip saves 'dismissed', both fire a window.dispatchEvent(new Event('rei_tour_update')) after writing to localStorage. Export getTourState, setTourStep, resetTour helpers. Use lucide-react for icons (X, CheckCircle2, SkipForward, PlayCircle). No Tailwind — use inline styles only.
File 2 — create apps/deallink/src/components/OnboardingProgressBar.jsx:
A floating progress bar. Fixed position, bottom-left corner (bottom: 20px, left: 20px). Reads the 5 step keys from localStorage key rei_flywheel_tour and listens to the rei_tour_update window event to re-render. Collapsed state: white pill with a mini progress bar (80px wide, 5px tall, gold fill) + 'X/5 done' text + chevron. Expanded state: shows a checklist card above the pill with each step name (Properties, Deal overview, Deal analysis, Documents, Investment memo), a checkbox (gold fill when complete), strikethrough + gray text when skipped, and a 'Redo' button on skipped steps that calls setTourStep(key, undefined). When all 5 are complete and collapsed: show a gold-bordered trophy pill 'Setup complete!' instead. Import getTourState and setTourStep from OnboardingCard.jsx. Use lucide-react (ChevronUp, Trophy). No Tailwind — inline styles only.
Edit apps/deallink/src/components/Layout.jsx:
Import OnboardingProgressBar from './OnboardingProgressBar.jsx' and render <OnboardingProgressBar /> just before the final closing div of the return statement.
Edit apps/deallink/src/pages/AdminDashboard.jsx:
Import OnboardingCard from '../components/OnboardingCard.jsx' and render <OnboardingCard stepKey='properties_list' /> as the very first child inside the Layout component's return.
Edit apps/deallink/src/pages/DealEditor.jsx:
Import OnboardingCard from '../components/OnboardingCard.jsx'. Find the tab bar block (the mode === 'edit' && existing && div with the 4 tab buttons). Right after that block closes, add: {mode === 'edit' && existing && <OnboardingCard stepKey={\deal_${tab}`} />}`
Do not change any other files. Do not change any styles on existing components