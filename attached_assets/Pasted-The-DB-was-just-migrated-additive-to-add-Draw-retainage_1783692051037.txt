The DB was just migrated (additive) to add Draw.retainagePct (Decimal, default 0), Draw.lienWaiverDocId (String?), and Draw.lienWaiverReceived (Boolean, default false). First update prisma/schema.prisma to match (add those three fields to Draw), then npx prisma generate. Read the existing Draw, ChecklistItem (+ ChecklistStatus), DrawStatus, ContractorComplianceDoc (+ versions, expiresAt) models, Company.coiThresholdDays, lib/paymentGate.ts, the draws approve/reject routes, and app/rehab/[projectId]/checklist/page.tsx. Build "Phase 2 — Draws & Compliance." Don't change the budget/forecast/invoice logic. Add auth + can() to every new route like the others.
1. Checklist CRUD + CSV import. On Checklist & Payments, let admins/PMs add, edit, rename, reorder, and delete ChecklistItems per job type, and mark each Pending/Done. Add a "Import CSV" option (paste text or upload a .csv) that creates one checklist item per row. New API route(s) under /api/rehab/[projectId]/checklist.
2. In-app draw creation. Add a "Create draw" action per job type on Checklist & Payments: fields = title, amount, retainage % (retainagePct), notes. Creates a Draw (status Pending) via a new create route. Keep the existing checklist→payment gate: releasing/approving a draw still goes through lib/paymentGate.ts (all checklist items must pass in strict mode). Show each draw's status and, using retainage %, display the retained (held-back) amount and the net payable.
3. Lien waiver gated to release. On a draw, allow uploading a lien-waiver document (store its id in lienWaiverDocId, set lienWaiverReceived = true). When strict mode is on, block marking a draw Paid until lienWaiverReceived is true (surface a clear message).
4. Contractor compliance alerts. Surface ContractorComplianceDoc expirations using expiresAt vs Company.coiThresholdDays: color-coded status (Expired = red, Expiring within threshold = amber, OK = green) for the project's assigned contractor, shown on the project (e.g. an alert on Checklist & Payments / Documents) so you can't unknowingly pay a contractor with lapsed insurance.
5. Draw package (printable). Add a "Draw package" view per draw that assembles, in a print-friendly layout with a Print button (browser Print-to-PDF — do NOT add a server-side PDF library): project + job type, draw amount / retainage / net, the job's checklist status, the invoices tied to that job type, and the lien-waiver status. Lender/partner-ready snapshot.
Then run prisma generate && next build, confirm no ESLint errors, and self-review that draws, the payment gate, and compliance don't regress existing behavior.